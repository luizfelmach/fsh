#ifndef UTILS_H
#define UTILS_H

/*
 * Macros used for debugging.
 */
#define DEBUG_STR(var) printf("DEBUG: %s = '%s'\n", #var, var)
#define DEBUG_INT(var) printf("DEBUG: %s = '%d'\n", #var, var)

#endif  // !UTILS_H
